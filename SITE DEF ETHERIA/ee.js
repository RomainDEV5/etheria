$(document).ready(function(){
    $('.carousel').slick({
      autoplay: true, // Activer le défilement automatique
      autoplaySpeed: 4000, // Définir la vitesse de défilement en millisecondes (facultatif)
      // Autres options de configuration ici
    });
  });